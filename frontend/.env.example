# Frontend Environment Variables

# API Base URL (optional - defaults are set in the code)
# For development, leave empty to use proxy configuration
# For production with separate backend server, set the full URL
# VITE_API_URL=http://localhost:5000/api

# Development mode (automatically set by Vite)
# NODE_ENV=development

# Production deployment URL (if different from default)
# VITE_BASE_URL=/